// SDK-FREE wire subpath (`@openmasq/llm/wire`). Same purpose as `pricing.ts`: the
// gateway prices AND meters token usage without bundling any provider SDK, from the
// exact parsers the desktop client uses. See `wire/index.ts`.
export * from "./wire/index.js";

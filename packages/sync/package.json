{
  "name": "@openmasq/sync",
  "version": "0.1.0",
  "description": "Cross-device end-to-end encrypted sync (record oplog and vaults), organisation scopes and audit — pure TypeScript shared by every client.",
  "license": "Apache-2.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.cjs",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "require": "./dist/index.cjs"
    }
  },
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "typecheck": "tsc --noEmit",
    "lint": "echo \"(no lint configured)\"",
    "clean": "rm -rf dist .turbo"
  },
  "devDependencies": {
    "@types/node": "^26.4.0",
    "tsup": "^8.3.0",
    "typescript": "^5.6.3"
  },
  "dependencies": {
    "@openmasq/branding": "workspace:*",
    "@openmasq/schema": "workspace:*"
  }
}

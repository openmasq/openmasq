/* The Lucide icon set, split by what a glyph MEANS rather than by shape (rule 2):
   `sections` = the marks that name a place in the app, `actions` = the verbs,
   `data` = the kinds/states. `Icon` is the shared 24×24 stroke-2 wrapper every one
   of them is drawn with. Import from this barrel (or `../brand`), not from a part. */
export { Icon } from "./Icon";
export * from "./sections";
export * from "./actions";
export * from "./data";

{
  "apps/api/src/oauth/redirectUri.ts::* port. Everything else must match a registered URI EXACTLY (no prefix tricks).": "* port. Everything else must match a registered URI EXACTLY (no prefix tricks).",
  "apps/auth/src/providers/slack.ts::/** base64url (no padding) — MUST match the desktop's `challengeOf` exactly. */": "/** base64url (no padding) — MUST match the desktop's `challengeOf` exactly. */",
  "apps/backend/src/config/app/migrations/20260630005_create-products.ts::// Local mirror of the Stripe product catalog. Lets the frontend render": "// Local mirror of the Stripe product catalog. Lets the frontend render",
  "apps/backend/src/features/members/members_commands/create/acceptInvite.ts::// MUST match the invite's target email — otherwise the invite is left UNTOUCHED": "// MUST match the invite's target email — otherwise the invite is left UNTOUCHED",
  "apps/backend/src/features/organizations/organizations_commands/update/validateUpdateOrganizationPayload.ts::// (defence in depth). Keep this list in sync with ORG_SETTINGS_ALLOWED_KEYS.": "// (defence in depth). Keep this list in sync with ORG_SETTINGS_ALLOWED_KEYS.",
  "apps/backend/tests/e2e/scenario_7_sync_records.ts::// The one contributor-readable scope — must match @openmasq/sync COFFRE_SCOPE.": "// The one contributor-readable scope — must match @openmasq/sync COFFRE_SCOPE.",
  "apps/desktop/src/main/db/schema.ts::/** Embedding dimension. Must match your embedding model (OpenAI 3-small = 1536). */": "/** Embedding dimension. Must match your embedding model (OpenAI 3-small = 1536). */",
  "apps/desktop/src/main/mcp/browser/agentMain.ts::// a mismatch between any of these is itself a bot tell, so they must agree.": "// a mismatch between any of these is itself a bot tell, so they must agree.",
  "apps/desktop/src/main/mcp/connectors/index.ts::* prefixes (kept in sync with `credGroupOf`'s `/^(gmail|google-)/`). */": "*  prefixes (kept in sync with `credGroupOf`'s `/^(gmail|google-)/`). */",
  "apps/desktop/src/preload/browserStealth.ts::// permissions: Notification query must agree with Notification.permission — a": "// permissions: Notification query must agree with Notification.permission — a",
  "apps/updates/src/features/desktop/desktop_commands/delete/pruneDesktopReleases.ts::* and the DB, so it's the one place that can keep the two in sync.": "*  and the DB, so it's the one place that can keep the two in sync.",
  "packages/catalog/src/mcp/authTag.ts::// modal only shows \"Mes clés\" here, and the chip must agree with the buttons.": "// modal only shows \"Mes clés\" here, and the chip must agree with the buttons.",
  "packages/emails/emails/Invite.tsx::// Team-invite email. The backend also keeps an inline-HTML mirror of this design": "// Team-invite email. The backend also keeps an inline-HTML mirror of this design",
  "packages/llm/src/models/openrouterCatalog.ts::* consume it and MUST agree (rule 9): the desktop merges it into the picker's registry": "* consume it and MUST agree (rule 9): the desktop merges it into the picker's registry",
  "packages/redact/scripts/gen-places.ts::// Countries we ship fakes for (extend as tables are added). Keep in sync with": "// Countries we ship fakes for (extend as tables are added). Keep in sync with",
  "packages/redact/src/documents/ocrPins.ts::* Latin / Cyrillic / Arabic / Devanagari / CJK scripts. Keep in sync with the": "*  Latin / Cyrillic / Arabic / Devanagari / CJK scripts. Keep in sync with the",
  "packages/redact/src/engine/geo/countries.ts::// to be faked to a CITY (France → \"Troyes\") — the mirror of the city → country bug": "// to be faked to a CITY (France → \"Troyes\") — the mirror of the city → country bug",
  "packages/redact/src/index.ts::// The curated deny-lists + CJK test, for consumers whose matching must agree with the": "// The curated deny-lists + CJK test, for consumers whose matching must agree with the",
  "packages/sync/src/convSync.ts::/** Internal (shared with `convSyncApply.ts` — emit and absorb must agree). */": "/** Internal (shared with `convSyncApply.ts` — emit and absorb must agree). */",
  "packages/tesseract2/src/core/langData.ts::* \"sha256-<base64>\"` (or a bare hex digest), the resolved traineddata bytes must match": "* \"sha256-<base64>\"` (or a bare hex digest), the resolved traineddata bytes must match",
  "packages/ui/src/containers/modals/redaction/RedactionRulesModal.tsx::be two things to keep in sync (rule 9), so neither the labels nor the category maps are": "be two things to keep in sync (rule 9), so neither the labels nor the category maps are",
  "packages/ui/src/containers/modals/viewers/doc/docFit.ts::/** Total horizontal padding of the sheet (both margins) — keep in sync with `.fv-page`. */": "/** Total horizontal padding of the sheet (both margins) — keep in sync with `.fv-page`. */",
  "packages/ui/src/containers/modals/viewers/ooxml/textStyle.ts::// than being re-declared per format (rule 9: one home, no \"keep in sync\" twins).": "// than being re-declared per format (rule 9: one home, no \"keep in sync\" twins).",
  "packages/ui/src/containers/shell/mobile/MobileSettingsScreen.tsx::* ⚠️ Deliberately NOT copied from the kit's mock: its inline redaction-rules toggles and": "* ⚠️ Deliberately NOT copied from the kit's mock: its inline redaction-rules toggles and",
  "packages/ui/src/hooks/modalGate.ts::* flagged as a dialog. Kept in sync across the two visibility owners. */": "*  flagged as a dialog. Kept in sync across the two visibility owners. */",
  "packages/ui/src/hooks/useBrowserTabs.ts::* Mirror of the agent browser's authoritative TAB LIST for the shell (the right": "* Mirror of the agent browser's authoritative TAB LIST for the shell (the right",
  "packages/ui/src/memory/cluster.ts::* One definition (rule 9): the hook and any future recall pass must agree, or the": "*  One definition (rule 9): the hook and any future recall pass must agree, or the",
  "packages/ui/src/pages/ChatWorkspace/BrowserPanel/useBrowserBounds.ts::* position it over `viewportRef`'s rectangle and keep it in sync.": "* position it over `viewportRef`'s rectangle and keep it in sync.",
  "packages/ui/src/pages/ChatWorkspace/writeConfirm.ts::* RENDERER mirror of the confirmation MODE (`standard` | `renforce`). The source of truth": "* RENDERER mirror of the confirmation MODE (`standard` | `renforce`). The source of truth",
  "packages/ui/src/pages/Settings/settingsIndex.ts::* Deliberately a hand-kept list of the settings worth FINDING, not a mirror of every": "* Deliberately a hand-kept list of the settings worth FINDING, not a mirror of every",
  "packages/ui/src/send/attachmentLayers.ts::/** Mirror of the fold's per-document clip — an enormous OCR layer must not blow the": "/** Mirror of the fold's per-document clip — an enormous OCR layer must not blow the",
  "packages/ui/src/send/generatedFiles.ts::* (`pythonSeeds.ts`) — REAL names in, since the seeds must match what the de-redacted": "*  (`pythonSeeds.ts`) — REAL names in, since the seeds must match what the de-redacted",
  "packages/ui/src/send/pythonSeeds.ts::const MAX_SEED_BYTES = 25 * 1024 * 1024; // mirror of the sandbox's per-file cap": "const MAX_SEED_BYTES = 25 * 1024 * 1024; // mirror of the sandbox's per-file cap",
  "packages/ui/src/send/toolResult.ts::// Fail-safe mirror of the user-message path: the forced VALUES also ride": "// Fail-safe mirror of the user-message path: the forced VALUES also ride",
  "packages/ui/src/state/redux.ts::// Keep the legacy flat list in sync too, so a rollback to the pre-tiling": "// Keep the legacy flat list in sync too, so a rollback to the pre-tiling",
  "packages/ui/src/state/store.ts::// auth resolves, so it must match what `applyPersistedTheme` already painted or": "// auth resolves, so it must match what `applyPersistedTheme` already painted or",
  "packages/ui/src/styles/redaction.css::is no per-cell animation-delay to keep in sync with a JS sequence — two clocks over one": "is no per-cell animation-delay to keep in sync with a JS sequence — two clocks over one",
  "packages/ui/src/styles/skills/split.css::/* Must match the column, or the catalog shows THROUGH the band as it scrolls. */": "/* Must match the column, or the catalog shows THROUGH the band as it scrolls. */",
  "packages/ui/src/styles/tabs/rail.css::mirror of the left `.nav-dock`: it is a child of `.app` (not the white workspace": "mirror of the left `.nav-dock`: it is a child of `.app` (not the white workspace",
  "packages/ui/src/suggestions/routineGeneric.ts::* \"must agree\" duplication root rule 9 forbids. Generated, a new connector has an idea": "* \"must agree\" duplication root rule 9 forbids. Generated, a new connector has an idea"
}
